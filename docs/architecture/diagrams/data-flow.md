# Data Flow Diagrams

> Auto-generated by Autonomous Knowledge Synthesis
> Last updated: 2026-02-24

## Overview

This document traces three critical data flows through the Volleyball Ranking Engine: the import pipeline (data entry), the ranking computation (core processing), and the export pipeline (data output). Together, these three flows represent the complete lifecycle of tournament data through the system.

## 1. Import Pipeline Flow

Data enters the system as XLSX spreadsheets and flows through parsing, identity resolution, validation, and database insertion.

```mermaid
flowchart LR
    subgraph "Input"
        XLSX[".xlsx File<br/>(Finishes or Colley)"]
    end

    subgraph "Phase 1: Parse & Resolve"
        Parse["Parser<br/>(FinishesParser /<br/>ColleyParser)"]
        Extract["Extract unique<br/>team codes +<br/>tournament names"]
        Resolve["IdentityResolver<br/>(exact match +<br/>Levenshtein fuzzy)"]
        Preview["Return ParseResult<br/>+ identity mappings<br/>+ conflicts<br/>+ duplicates"]
    end

    subgraph "User Decision"
        UI["Committee resolves<br/>conflicts:<br/>map / create / skip"]
    end

    subgraph "Phase 2: Validate & Write"
        Validate["Validate rows<br/>against Zod<br/>insert schemas"]
        Decision{Import Mode?}
        Replace["Atomic Replace<br/>(PostgreSQL RPC)"]
        Merge["Row-by-row<br/>Merge<br/>(SELECT + INSERT/<br/>UPDATE / SKIP)"]
        Summary["ImportSummaryData<br/>(counts)"]
    end

    subgraph "Database"
        TR[("tournament_results")]
        RR[("ranking_results")]
    end

    XLSX --> Parse
    Parse --> Extract
    Extract --> Resolve
    Resolve --> Preview
    Preview --> UI
    UI --> Validate
    Validate --> Decision
    Decision -->|"replace"| Replace
    Decision -->|"merge"| Merge
    Replace --> TR
    Replace --> RR
    Merge --> TR
    Merge --> RR
    Replace --> Summary
    Merge --> Summary
```

### Data Transformations

| Stage | Input | Output | Key Operation |
|-------|-------|--------|---------------|
| Parse | `ArrayBuffer` (XLSX binary) | `ParseResult<ParsedFinishesRow>` | Scan Row 1 merged cells for tournament names, detect Div/Fin/Tot triplets in Row 2, extract team+tournament data per row |
| Identity Resolve | `string[]` (team codes, tournament names) | `{matched: Map, unmatched: IdentityConflict[]}` | Case-insensitive exact match, then Levenshtein fuzzy match with >0.3 threshold |
| Validate | `ParsedFinishesRow[]` + `IdentityMapping[]` | `ValidatedRow[]` | Apply identity mappings (parsedValue -> UUID), validate against `tournamentResultInsertSchema` |
| Replace | `ValidatedRow[]` | `void` (database rows) | `import_replace_tournament_results` RPC: DELETE all for season+age_group, INSERT all new rows atomically |
| Merge | `ValidatedRow[]` | counts | Per-row: SELECT existing by composite key, INSERT if new, UPDATE if changed, SKIP if identical |

## 2. Ranking Computation Flow

Stored tournament data flows through the ranking pipeline, producing normalized results for five algorithms.

```mermaid
flowchart TB
    subgraph "Data Fetch"
        Teams["SELECT teams<br/>WHERE age_group = X"]
        Tourns["SELECT tournaments<br/>WHERE season_id = X"]
        Weights["SELECT tournament_weights"]
        Source{"Match records<br/>available?"}
        Matches["SELECT matches"]
        Finishes["SELECT tournament_results"]
    end

    subgraph "W/L Derivation"
        DeriveMatch["deriveWinsLossesFromMatches()<br/><i>Direct match results</i>"]
        DeriveFinish["deriveWinsLossesFromFinishes()<br/><i>Pairwise from finish positions:<br/>lower position beats higher</i>"]
        Flatten["flattenPairwiseGroups()<br/><i>Merge all tournaments<br/>into flat PairwiseRecord[]</i>"]
    end

    subgraph "Algorithm Execution"
        Algo1["algo1: Colley Matrix<br/><i>Flat records, tournament weights<br/>scale matrix entries</i>"]
        Algo2["algo2: Elo (start=2200)<br/><i>Chronological groups,<br/>K=32 * weight</i>"]
        Algo3["algo3: Elo (start=2400)"]
        Algo4["algo4: Elo (start=2500)"]
        Algo5["algo5: Elo (start=2700)"]
    end

    subgraph "Normalization"
        Norm["normalizeAndAggregate()<br/><i>Min-max to 0-100 per algo,<br/>AggRating = mean(5 scores),<br/>AggRank by AggRating desc</i>"]
    end

    subgraph "Supplementary"
        Seed["computeSeedingFactors()<br/><i>Win %, best Tier-1 finish</i>"]
    end

    subgraph "Persistence"
        Insert["INSERT ranking_results<br/>(batch, 12 columns per team)"]
    end

    Teams --> Source
    Tourns --> Source
    Weights --> Source
    Source -->|"Yes"| Matches
    Source -->|"No"| Finishes
    Matches --> DeriveMatch
    Finishes --> DeriveFinish
    DeriveMatch --> Flatten
    DeriveFinish --> Flatten

    Flatten --> Algo1
    DeriveMatch --> Algo2
    DeriveMatch --> Algo3
    DeriveMatch --> Algo4
    DeriveMatch --> Algo5
    DeriveFinish --> Algo2
    DeriveFinish --> Algo3
    DeriveFinish --> Algo4
    DeriveFinish --> Algo5

    Algo1 --> Norm
    Algo2 --> Norm
    Algo3 --> Norm
    Algo4 --> Norm
    Algo5 --> Norm

    Norm --> Insert
    Flatten --> Seed
    Seed --> Insert
```

### Algorithm Data Requirements

| Algorithm | Input Structure | Key Difference |
|-----------|----------------|----------------|
| Colley Matrix | `PairwiseRecord[]` (flat, all tournaments merged) | All games weighted equally by time; tournament weights scale matrix entries |
| Elo (4 variants) | `TournamentPairwiseGroup[]` (grouped by tournament, sorted chronologically) | Processes tournaments in date order; later tournaments influence final rating more |

### Normalization Pipeline

```mermaid
flowchart LR
    Raw1["algo1 raw ratings<br/>(Colley: ~0.2-0.8)"]
    Raw2["algo2 raw ratings<br/>(Elo-2200: ~1800-2600)"]
    Raw3["algo3 raw ratings<br/>(Elo-2400: ~2000-2800)"]
    Raw4["algo4 raw ratings<br/>(Elo-2500: ~2100-2900)"]
    Raw5["algo5 raw ratings<br/>(Elo-2700: ~2300-3100)"]

    Norm1["algo1 normalized<br/>(0-100)"]
    Norm2["algo2 normalized<br/>(0-100)"]
    Norm3["algo3 normalized<br/>(0-100)"]
    Norm4["algo4 normalized<br/>(0-100)"]
    Norm5["algo5 normalized<br/>(0-100)"]

    Agg["AggRating =<br/>mean(norm1..norm5)"]
    Rank["AggRank =<br/>sort desc,<br/>ties: alpha by name"]

    Raw1 -->|"min-max"| Norm1
    Raw2 -->|"min-max"| Norm2
    Raw3 -->|"min-max"| Norm3
    Raw4 -->|"min-max"| Norm4
    Raw5 -->|"min-max"| Norm5

    Norm1 --> Agg
    Norm2 --> Agg
    Norm3 --> Agg
    Norm4 --> Agg
    Norm5 --> Agg
    Agg --> Rank
```

## 3. Export Pipeline Flow

Ranking results flow from the API through data assembly and format-specific generators to downloadable files.

```mermaid
flowchart LR
    subgraph "API Response"
        Results["ranking_results<br/>(per team: 12 algo columns)"]
        Teams["teams<br/>(name, region)"]
        Overrides["ranking_overrides<br/>(original_rank, final_rank,<br/>justification, member)"]
        SeedFactors["Seeding factors<br/>(win_pct, best_national_finish)"]
        RunMeta["Ranking run metadata<br/>(season, age_group, ran_at,<br/>status)"]
    end

    subgraph "Data Assembly (Pure Functions)"
        FinalRank["computeFinalRanks()<br/><i>Merge agg_rank +<br/>overrides</i>"]
        Assemble["assembleExportRows()<br/><i>Flat ExportRow[] sorted<br/>by final_rank</i>"]
        Headers["getColumnHeaders()<br/><i>Dynamic columns based<br/>on options + overrides</i>"]
        Meta["buildMetadataLines()<br/><i>Season, age group,<br/>timestamp</i>"]
    end

    subgraph "Format Generators"
        CSV["generateCsv()<br/><i>RFC 4180 with<br/>comment header</i>"]
        XLSX["generateXlsx()<br/><i>Sheet 1: Rankings<br/>Sheet 2: Overrides</i>"]
        PDF["generatePdf()<br/><i>Auto-table with<br/>page numbers</i>"]
    end

    subgraph "Download"
        Blob["Blob / ArrayBuffer"]
        Trigger["triggerDownload()<br/><i>Hidden anchor click</i>"]
        File["rankings_18U_2026-02-24.csv<br/>rankings_18U_2026-02-24.xlsx<br/>rankings_18U_2026-02-24.pdf"]
    end

    Results --> Assemble
    Teams --> Assemble
    Overrides --> FinalRank
    FinalRank --> Assemble
    SeedFactors --> Assemble
    RunMeta --> Meta

    Assemble --> CSV
    Assemble --> XLSX
    Assemble --> PDF
    Headers --> CSV
    Headers --> XLSX
    Headers --> PDF
    Meta --> CSV
    Meta --> XLSX
    Meta --> PDF

    CSV --> Blob
    XLSX --> Blob
    PDF --> Blob
    Blob --> Trigger
    Trigger --> File
```

### Export Column Layout

The export includes a base set of columns plus optional algorithm breakdowns and override details:

| Column Group | Columns | Condition |
|-------------|---------|-----------|
| **Core** | Final Rank, Team, Region, Agg Rating, Agg Rank, Win %, Best National Finish | Always included |
| **Algorithm Breakdowns** | Algo 1-5 Rating, Algo 1-5 Rank (10 columns) | When `includeAlgorithmBreakdowns` is true |
| **Override Details** | Override Original Rank, Override Justification, Override Committee Member | When overrides exist for any team |

### Format-Specific Details

| Format | Library | Characteristics |
|--------|---------|----------------|
| CSV | None (pure string) | RFC 4180 compliant, metadata as `#` comment lines, override summary section appended |
| XLSX | `xlsx` (dynamic import) | Two worksheets: "Rankings" (metadata + data) and "Overrides" (summary, conditional) |
| PDF | `jspdf` + `jspdf-autotable` (dynamic import) | Landscape for detailed reports, portrait for summary. Header: title + metadata. Footer: page numbers. Override summary as second auto-table. |
